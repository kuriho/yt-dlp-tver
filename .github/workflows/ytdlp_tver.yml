name: TVer

on:
  schedule:
    - cron: '0 20 * * *'  # Customize the cron schedule as desired
  workflow_dispatch:

jobs:
  sparse-checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Monorepo
        uses: dfdez/checkout@feat/sparse-checkout
        with:
          repository: yt-dlp/yt-dlp
          ref: master
          path: lib
          fetch-depth: 1
          sparse-checkout: |
            --no-cone
            yt_dlp/* 
            !yt_dlp/extractor/*

      - name: List Cloned Files
        run: |
          ls -R

      - name: Sparse Checkout
        run: |
          git sparse-checkout init --cone
          git sparse-checkout set yt_dlp/* !yt_dlp/extractor/* --no-cone
          git sparse-checkout add yt_dlp/extractor/brightcove.py yt_dlp/extractor/tver.py --no-cone
        working-directory: lib


      - name: List Cloned Files 2
        run: |
          ls lib/yt_dlp/extractor
          ls -R
            
      - name: Copy Files
        run: |
          mkdir -p $GITHUB_WORKSPACE/lib/yt_dlp
          cp -r yt_dlp/* $GITHUB_WORKSPACE/lib/yt_dlp/
          git -C $GITHUB_WORKSPACE add lib/yt_dlp
        working-directory: lib

      - name: Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Update sparse checkout
          commit_user_name: GitHub Actions
          commit_user_email: actions@github.com
